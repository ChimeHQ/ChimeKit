name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - '**'

jobs:
  test:
    name: Test
    runs-on: macos-12
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: swift build
