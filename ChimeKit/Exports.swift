// These both need to be compiled with "-enable-library-evolution". However
// that's only possible with unsafeFlags, which then makes them not work
// as dependencies.
@_exported import ChimeExtensionInterface
@_exported import ChimeLSPAdapter
